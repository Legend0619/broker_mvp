/** @type {import('next').NextConfig} */
const nextConfig = {
  basePath: "/find-a-broker",
  assetPrefix: "/find-a-broker/",
  reactStrictMode: true,
  output: "export",
};

module.exports = nextConfig;
